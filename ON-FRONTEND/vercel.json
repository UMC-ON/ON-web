{
  "rewrites": [
    {
      "source": "/live/:path*",
      "destination": "/index.html"
    }
  ],
  "env": {
    "BRANCH": "DEPLOYMENT"
  },
  "routes": [{ "src": "/[^.]+", "dest": "/", "status": 200 }]
}

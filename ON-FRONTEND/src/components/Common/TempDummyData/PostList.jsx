import { Post } from './Post';
import * as s from './CommentAndReply';

export const PostList = [Post];
export const CommentList = [s.Comment, s.Comment, s.Comment];
export const ReplyList = [s.Reply, s.Reply];
